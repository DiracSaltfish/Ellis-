# 前 10 标的来源记录

抓取日期：2026-09-09  
输入文件：`/Volumes/Upan/premiumWatchlistSymbols.csv`  
取值规则：按原始 CSV 顺序取前 10 个标的，代码去掉 `.SZ`/`.SH` 后查询。

## 输入前 10 个标的

1. `159217.SZ`
2. `520700.SH`
3. `159365.SZ`
4. `159570.SZ`
5. `513920.SH`
6. `513830.SH`
7. `159303.SZ`
8. `513910.SH`
9. `159920.SZ`
10. `159040.SZ`

## 每个标的保存内容

每个代码目录均保存：

- `<code>_eastmoney_issuance_operation_notices.json`：东财 `JJGG` 接口 `type=1`（发行运作）结果，包含分页公告和最新更新招募书筛选结果；159040 另记录 `latest_prospectus_any`，用于保留初始招募书线索。
- `<code>_<招募书>.pdf`：用于结论的招募说明书 PDF。
- `<code>_<招募书>.txt`：PDF 文本提取结果，用于关键词检索和复核。
- `<code>_pcf_basic_20260909.html`、`<code>_pcf_components_20260909.html`：新浪 PCF 页面暂存。
- `<code>_pcf_2026-09-09_basic.csv`、`<code>_pcf_2026-09-09_components.csv`：PCF 页面解析结果。

## 来源分层

- A 级：基金管理人、深交所/上交所、中国结算正式规则或文件。
- B 级：中国结算信息披露镜像、官方投教/技术材料。
- C 级：新浪等第三方 PCF 镜像，仅用于交叉校验。

本批更新招募书 PDF 通过东财公告记录定位并下载保存；PCF 使用新浪镜像暂存。批量正式运行时，应优先替换为基金管理人、交易所或中国结算的 PCF 原文件，并保存抓取日期、T 日和文件 hash。

## 关键处理口径

- 只把标题命中“更新招募说明书”的记录填入 `latest_updated_prospectus_date`；“基金产品资料概要更新”单独记录，不能替代招募书。159040 截至本次查询只找到初始招募书，因此更新字段留空。
- `creation_buy_deadline`/`redemption_sell_deadline` 只表示基金管理人代投资者买入/卖出被替代证券的处理时点；T+3、T+5、T+7 等退补款期限另列。
- 只要权威来源写到“日间 RTGS 交收”，`rtgs_subscription` 即为 `PASS`；资金足额、勾单和日终回退方式为备注。
- 基金管理人底层同日申赎轧差只有在该基金文件明确写出时才判 `PASS`。泛化规则、风险提示或其他基金文件不能替代本基金证据。
