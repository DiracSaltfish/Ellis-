# 15｜2026-08-27 全量重编译与发布记录

## 结论

当前合并源码对应的本机 macOS arm64 Debug、Release、发布目录和发布 zip 已全部重新编译。合并内容同时包含 A-console 实时告警/历史审计、B 提示音试听与预置、可写配置路径修复、详情关闭生命周期修复，B 独立信号/全局双列表，A-console 额外 L1 热维护页签，以及深港通 `02800.HK` 精确路由支持。自动化没有发送任何 QMT 交易或撤单请求；发布包 B 仅以 `--read-only` 和不可达测试端口做启动/UI 冒烟。

Windows 仍只有共用源码和 `windows-msvc-release` Preset。由于当前没有 Windows/MSVC 主机，本记录不把 Windows 安装包标记为已编译或已验证。

## 本机产物

| 产物 | 路径 | 结果 |
| --- | --- | --- |
| Debug | `build/macos-arm64-debug-make` | clean rebuild 成功 |
| Release | `build/macos-arm64-release-make` | clean rebuild 成功 |
| 发布目录 | `dist/macos-arm64` | 重新安装、macdeployqt、稳定 Apple Development 签名成功 |
| 发布 zip | `dist/ETF溢价率拉升监控-macOS-arm64.zip` | 重新生成，约 72 MB |
| 校验文件 | `dist/SHA256SUMS` | 校验通过 |

发布 zip 的最终 SHA-256 以同目录 `dist/SHA256SUMS` 为准；校验文件位于 zip 外部，避免把 zip 自身哈希写回包内造成循环变化。

三个主程序均为 arm64：`etf-premium-core`、`etf-premium-console`、`etf-premium-client`。客户端版本为 `0.2.0`，当前 Homebrew QtWebSockets/QtMultimedia/zstd 的最严格依赖下限为 macOS 26.0，因此 Preset、Mach-O `minos` 和 Info.plist 均固定为 26.0，避免声称并不真实的旧系统兼容性。

## 测试结果

- 本机 Debug CTest：5/5；
- 本机 Release CTest：5/5；
- 本机 Python pytest/unittest：15/15；
- machome Release CTest：2/2；
- machome Python unittest：10/10；
- adapter/tools/tests `py_compile`：通过；
- 两个 GUI bundle `codesign --verify --deep --strict`：通过；
- core `codesign --verify --strict`：通过；
- zip `shasum -a 256 -c SHA256SUMS`：通过。

发布包客户端确认识别 `--server`、`--config`、`--settings`、`--read-only`。发布包确认包含 `config/l1_hotlist.json`、`enable_hkt_l1=true`、B 端 `NSLocalNetworkUsageDescription` 和当前全部文档。以 `--read-only` 和独立临时设置启动 UI 后，确认首页默认信号列表、可手工切换全局列表；测试结束后已关闭，临时目录移入废纸篓。此前局域网验收已证明本机可建立到 `192.168.1.113:8421` 的 summary/detail 两条连接。

## 打包修复

本次新增统一 Info.plist 模板和可重复执行的 `tools/package_macos_arm64.sh`：

- B/A bundle 均包含 `NSLocalNetworkUsageDescription`；
- `macdeployqt` 后优先使用 Keychain 内有效代码签名身份对整个 bundle 重签；无有效身份时才回退 ad-hoc。稳定签名可避免每次重编导致 macOS 局域网权限身份变化；
- 发布配置只包含 `app.example.json`、`client-settings.example.json`、`tgw_account.example.ini`、观察清单和名称表；
- 明确排除生产 `app.json`、`client-settings.json`、`tgw_account.ini`、`tgw_username_override`；
- 发布包包含当前全部文档和诊断工具。

Finder 首次启动时的 macOS 局域网授权必须由操作者本人批准，自动化未代替点击。GUI `.app` 已携带 Qt 运行库；发布目录中的独立 A-core 仍要求服务端主机安装当前 Homebrew Qt/zstd，machome 已满足该条件。

## machome 状态

合并源码、发布 zip 和安装目录已同步到 `/Users/ellis/工具程序开发/溢价率拉升监控` 与 `/Users/ellis/Applications/ETF溢价率拉升监控`。安装更新采用同盘备份后原子替换，备份位于 `.release-backups/20260827-merged-final`。桌面替身继续指向安装目录的新 A-console。

在 14:24:52 的二进制替换窗口，原监管器触发了一次 A-core/adapter 自动恢复；14:24:56 core 重新监听 8421/19195，14:24:57 TGW 重新登录并接受订阅，日志证据对应约 4.4 秒行情链路窗口。随后的 8 秒局域网验证为：203 个 ready 标的、0 隔离、`adapter_gaps=0`、`sdk_queue_depth=0`，summary 和 detail 均无断线，详情首帧约 3.7 ms，后续快照约 3 秒一帧。ETF 申购赎回监控和 Webull 行情转发器全程保持运行，本次没有发送任何 QMT 委托。

当前生产 core 是自动恢复时加载的上一签名映像（与当前 core 源码功能相同，文件随原子替换移入上述备份目录）；下次受控重启时将加载安装目录内的最终签名 core。新 A-console 已以 `--no-autostart` 旁路运行并连接 8421；该模式的启动、停止、重启、盘外行情和回放按钮均已禁用，不接管旧进程监管关系。
