# 保留的部署版本

- `current/`：2026-09-08 已部署 UI 改版，工作区未提交源码已保留。
- `previous/`：2026-09-07 上一版，供回滚使用。

每个目录保存完整应用 ZIP、SHA-256、版本清单和可用的包内源码哈希清单。清理前已检查 ZIP 全部条目 CRC，并核对复制前后的 SHA-256。ZIP 仅本地保存，由本目录 .gitignore 排除，不随 Git 推送。

解压使用 macOS `ditto -x -k`，部署沿用 app/scripts 与既有验收文档，保留实际运行配置与数据；不要直接以示例配置替换线上配置。源码重建需要现有 Qt、IBKR SDK、protobuf/abseil 等构建依赖。

本次清理只涉及本地旧构建/候选发布目录，不更改 machome 应用、配置、登录、数据或远端回滚副本。
