# DMIT 修复包（已于 2026-09-07 11:30 部署）

此包从四合一内置的完整业务代码构建，基于 9 月 4 日主线的业务内容，包含现有 CFETS 更新、白银东方财富结算来源、监控去抖和任务回执修复。不能从 8 月 26 日的 99aaae4 工作树重新打包覆盖。

全量 Go 测试已通过；manifest.json 记录二进制及每个 Go 输入文件 SHA-256。部署前必须核对当前线上二进制和服务配置，备份二进制、前端、数据目录及数据库，保留当前告警暂停状态；部署后只验证健康、上传回执和展示接口。禁止买入、卖出、申购、赎回调用。

用户已明确授权本次 DMIT 部署，部署回执见 ../dmit-active-release.json。指数四组真实上传已恢复；白银需要在 13:30 采集窗口确认新的上传回执，目前已验证恢复识别数据库中原东方财富来源。Machome 使用 current 协议，无需旧版兼容模式。
