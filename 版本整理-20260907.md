# 2026-09-07 源码归档与版本核对

本次将工作区中尚未纳入 Git 的开发成果归档到 main。未执行部署、线上服务重启或交易请求；“版本相符”指与此前保存的发布清单核对，不代表所有业务缺口均已修复。

## 最新版本定位

- 当前四合一入口：`machome四合一运行改造cpp/app`。`machome四合一运行` 是较早实现与迁移参考，不能作为替代发布入口。
- 后台基线：`1.0.0-unversioned-dirty-20260907T033508Z`（9 月 7 日 R4）。界面版本：`ui-20260907-overview-zh`。本轮界面发布仅更新 UI，未替换后台。
- R4 清单中的 455 个文件与本地比对：448 个一致；7 个变化为 UI 的 CMake、MainWindow、ModulePages、界面测试，以及本次修正的 Upload 测试替身。新增界面文件由独立 UI 发布清单追踪，11 个 UI 源文件哈希全部匹配已发布界面。
- DMIT 修复包保存的 148 个 Go 源文件哈希与 `/Users/ellis/newnavnav` 全部一致；此前回迁的 26 个文件也全部匹配同步清单。
- 四合一 `app/components/upload/business` 与 newnavnav 的 271 个对应文件逐字节一致；组件额外保留 `BASELINE-SHA256.json` 作为历史来源记录。
- `newnavnav-preopen` 的旧提交 `99aaae4587ae6fd14f8bd040fc5a847ff0d0955a` 不是本次使用的版本。本次从 newnavnav main 的 `481ff66fb464270e026389686bf3789cc60bbadd` 连同未提交修复整理。

## 纳入版本控制的内容

| 目录 | 内容与定位 |
| --- | --- |
| machome四合一运行改造cpp | 四模块整合、内置 Upload 业务、部署/回滚工具、协议契约、测试、最新中文卡片界面与验收文档 |
| machome四合一运行 | 早期四合一实现与迁移参考，保留历史演进 |
| 溢价率拉升监控cpp | PushPlus 批处理、防抖/限流、配置模板与测试；不改变已部署进程 |
| database Cpp | 原生 TGW 客户端、协议实现及离线验证 |
| database for armmac_V2 | ARM 适配、接口边界、脱敏证据、测试、厂商参考头文件/手册及已标记的实验运行库 |
| 港股通相关性分析 | 研究与验收脚本、笔记本、结论文档；outputs/历史归档中的脚本与 Markdown 也保留 |
| analysis_520600、临时数据及根目录分析脚本 | 分析源码与说明，生成的数据输出留在本地 |

## 本地保留、不提交

`.gitignore` 排除构建/分发目录、node_modules、运行状态、日志、真实快照、应用回滚副本、研究原始数据和大型生成表格。本次没有删除这些文件，也没有清理用户历史数据。厂商原始头文件通过 `.gitattributes` 保留字节与换行，避免 Git 自动转换破坏参考哈希。

部分历史文件含 Markdown 双空格换行、厂商原始空白或旧文件末尾空行。本次没有为了格式检查批量改写这些来源文件，以保留其版本可追溯性。

## 本轮检查

- 四合一现有 Release 测试构建：CTest 37/37 通过，构建配置 `MACHOME_ENABLE_LIVE_QMT_ORDERS=OFF`。
- 独立拉涨监控：重新构建 PushPlus 测试目标并通过对应 CTest。
- database Cpp：协议 CTest 1/1 通过。
- ARM V2：177 项单测执行，通过，2 项跳过；未运行线上 SDK 验证。
- newnavnav：Go 全量测试与前端类型检查/生产构建通过；Python 估值 59、预开盘/协议 9、监控 33、XOP 家族 12 项通过。估值测试使用已安装 ib_insync 的 miniconda Python。
- 修正了两处仓库副本中的 `test_upload_health_monitor.py`：测试替身接受 `paused_scopes`，并验证日经收盘前后暂停范围。不改变业务代码。
- newnavnav 按 AGENTS 要求完成 GitNexus 上游影响查询和全量变更分析。测试替身图结果 UNKNOWN，已以全文引用核对其仅在测试中使用；整体已有变更涉及入口/估值公共链，图风险 critical，不能解释为“无影响”。

## 后续发布要求

从提交后的源码重新构建时生成新的 Git/build/source 身份；不要把本次 Git 提交描述成已更新 machome 或 DMIT 二进制。旧发布哈希只证明本次核对基线，后续发布仍需核对真实 ACTIVE_RELEASE 与包内清单。

四合一 UI 与协议验收的详细记录见 `machome四合一运行改造cpp/docs/ui-redesign-20260907`、`docs/client-protocol-audit-20260907` 和 `docs/live-20260907`。文档中引用的本地快照、日志、构建包可能被有意排除，需在本机查看；它们不是克隆仓库后默认可用的数据。
