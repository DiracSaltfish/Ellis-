# A 端 V32 配套正式切换与验收

2026-09-09，正式机 192.168.1.113。补丁 a-v32-20260909-143704，Agent PID 71188，IOPV run_id 1788935831885188000。

已更新正式 Agent、IOPV Go 服务及匹配的 Wind 辅助程序。沿用原配置、Qt 框架和已批准的 Wind ABI 探针。Agent 的 Qt 链接改为包内路径；初次启动失败已自动回滚，修正打包后重新切换成功。Wind 容器权限经用户授权后恢复采集。

## 已确认
- Agent control_ready/audit_ready 均为 true，unresolved_modules 为空。
- 原 L1 19195：订阅 ACK、国内/港股行情、ping/pong；旧 v2 摘要可连接。
- v3 summary：hello、sync_begin、197 个摘要、sync_complete。
- v3 detail：按标的订阅；本地 IOPV 与 valuation.midpoint_iopv 的 E6 转换一致，exchange_iopv_e6 保持独立。
- 120 只当日 PCF；744/750 个报价，恢复至切换前覆盖水平。86 只中间价/结算估值有效，其他缺失按质控门禁保留空值。
- 513090.SH 三种估值恢复；汇率源短时 cfets_unavailable 后已恢复，不填造缺失值。
- 分钟记录持续增加：切换后 written_rows 从 240 增至 1560；513090 当日历史由 250 增至 261 行。
- SQLite quick_check=ok；80 端口网页、阈值读取正常，默认阈值未改。
- 下一次 PCF 计划 2026-09-10 08:40（北京时间）。
- Wind 申赎采集 monitoring=true、ok=true；实盘订单能力 live_orders_allowed=false。

## 已有/独立异常
上传模块仍为 degraded，包含原有外部数据权限/进程问题；Webull 有非目标 MQTT 消息记录。此次不将这些状态描述为全部正常。

## 安全与回退
测试仅进行了读取、行情订阅、状态查询和恢复采集监控，未发送实盘申赎、买卖或撤单。
备份：/Users/ellis/Library/Application Support/MachomeHub/backups/a-v32-20260909-143704，包含原应用、modules.json、LaunchAgent 和 SQLite 在线备份。

B 端表格优化需重新打开 V32；当前运行旧进程未被强制关闭。
