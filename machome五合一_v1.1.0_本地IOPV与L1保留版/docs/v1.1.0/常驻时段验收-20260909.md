# 常驻时段修正与正式机验收

2026-09-09，北京时间。正式机 192.168.1.113，网页 http://192.168.1.113/。

## 时段与变更

| 数据/模块 | 自动时段和行为 |
| --- | --- |
| A 股行情 | 09:15 开始；15:00 收盘后留 1 分钟接收末笔，15:01 释放 IOPV 订阅 |
| 港股通行情 | 从原 09:30–16:09 调整到 09:15–16:10；16:10 释放订阅 |
| IOPV 计算和分钟记录 | 工作日图表时段 09:30–16:08；12:00–13:00 压缩午休；盘外不生成新估值 |
| IOPV PCF | 工作日 08:40；缺失每 5 分钟重试，截止 16:08；盘后启动不自动抓取；启动先校验并恢复本地当日 PCF，不阻止次日正常拉取 |
| 汇率 | 工作日 08:40–16:10 轮询；盘外停止自动请求 |
| 午夜换日 | 清空实时篮子、行情、汇率和最新快照；保留按日期落盘的档案；下一时段重新订阅 |
| Webull | 正式配置从 force_running 改为 auto，运行中控制命令成功；恢复已有北京时间工作日 09:00–16:00 自动采集窗口 |
| 申赎 | 现有 09:15:30–15:00 监控和 15:00 关闭流程；PCF 缓存窗口 08:30–23:00，已缓存跳过、失败至少间隔 15 分钟且每天最多 8 轮 |
| Upload / 同步 | Upload 当前按原计划空闲；同步、网页和 API 监听继续常驻 |

Go 共享 L1 客户端此前全天持有订阅，现按符号所属市场过滤；每秒检查边界并释放连接，空闲不拨号重连。原生 C++ TGW 原有取消订阅和关闭逻辑保留，扩大港股通窗口。原 L1 对外端口和订阅协议保留。

## 验收证据

- `session-tests.log`：Go 全包 race 测试通过（含换日、订阅时段、PCF 时段与本地缓存日期校验）；go vet 通过。
- `session-native-tests.log`：C++ premium-engine-domain / premium-engine-l1 回归通过。
- `session-acceptance.json`：21:47 正式接口返回 scheduled_idle，active_subscriptions=0、session=0、frames=0、fx_polling=false、errors={}；PCF 下次 2026-09-10 08:40；恢复 121 个本地篮子；513090.SH 当日历史 340 行可读。
- `session-production-check.txt`：Agent control_ready/audit_ready=true，无 unresolved_modules；A/HK quotes_desired=false；L1 hello/pong 正常；Webull、Upload、申赎按计划空闲。
- 网页 index.html、app.js、style.css、ledger.json 与本地 services/iopv/web 四个文件逐字节一致，SHA-256 见 session-acceptance.json。
- 正式配置备份：MachomeHub/config/modules.before-session-auto-20260909.json。Webull 运行中修改通过持久化命令和新鲜状态确认。

## 部署处理

首次 Agent 增量打包遗漏 Qt 依赖重定位，立即回退；现已将所有 Homebrew Qt 加载路径改为随 app 携带的 Frameworks，并单独签名变更的 Agent/Go helper，再签外层。第二轮检查发现旧子进程端口退出竞态并回退，随后切换成功；部署脚本已加入旧监听退出等待、新健康字段检查及失败回退。没有重新签名未变更的 Wind 探针。最终包校验见 session-package.sha256。

## 限制与未冒充完成的项目

- 时间边界采用模拟时钟测试，尚未自然观察明早 09:15 和下一次午夜。
- 目前自动日历为周一至周五，尚未接入上交所/深交所/港股通节假日和半日市日历；工作日休市仍可能在上述窗口内请求，不能宣称节假日自持已经完备。
- Webull 保留先前的 type-109 深度数据异常记录；本次恢复自动空闲，未将其数据质量错误标成已修复。
- 操作员手动强制采集/手动刷新仍可覆盖自动时段。
- 本次修改位于五合一 v1.1.0 源码及其正式包；原四合一和独立“内网IOPV计算”目录保持原样。
