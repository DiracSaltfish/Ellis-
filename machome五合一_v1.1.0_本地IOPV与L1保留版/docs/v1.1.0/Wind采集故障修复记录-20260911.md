# Wind 采集故障核查与修复记录

日期：2026-09-11，machome（macOS 26.6 / 25G72）。仅调整已核验的 ABI manifest 与运行状态，未替换应用二进制、未触发交易。

## 已核实的故障

1. **UI 与 Agent 配置不一致。** 原 Agent 的配置 SHA-256 为 `0a9b8f75c6ee3d34e614668107b0020429d41c8a0f29db646a495d96d4199bc3`，磁盘配置为 `91bb8cc10ac23ff0744ba96c0e08b03f2b7c6459b7715c5d92efc15db2098429`。审计正常，但 `client_config_bound=false`。原配置于 Agent 启动后修改；统一重启解决了绑定问题。
2. **探针重新签名后清单未同步。** 清单仍批准旧文件 `aac0136589b57e1777a36824d5c66c7bf1890816c2a4dee01ca09d168a461b9d`，部署文件实际为 `6b23796ec474313a5cc9a633495de6b789a296b667468fc5b7fab0d15652a167`，导致硬性拒绝采集。
3. **旧关闭命令留下未知结果锁。** 命令 `38e15ee3-8520-4e79-938d-aef9acf9f8e9` 在 14:10:21 请求关闭 Wind 后超时，重启时按未知结果锁定申赎模块。通过核实旧进程树全部退出、旧 helper 已回收、新 Wind/helper 进程身份明确，以自带 `acknowledge_uncertain` 两阶段协议记录证据并解锁；未直接修改审计数据库。
4. **其他 App 数据权限阻塞。** 14:25:28 对 helper PID 15621 采样，主线程持续停在 `liveBoundaryReady → QFile::setPermissions → chmod → __chmod`。尚未进入 LLDB 订阅。用户此时反馈连续出现授权弹窗。系统 TCC 未查到 `com.ellis.machome.operations-hub` 的完全磁盘访问允许条目；独立版存在允许条目。

## 探针核验依据与变更

对照文件：`~/Applications/MachomeCandidates/1.1.0-production-20260909/Machome 四合一运行中心.app/Contents/Helpers/libmachome_wind_tbapi_probe.dylib`。它的完整哈希恰好匹配旧清单。

只在临时副本上移除签名后逐字节比较：两个文件均为 50,504 字节，仅偏移 841 的一个字节不同，属于 `__LINKEDIT` 的 vmsize（16,384 → 32,768）。代码、常量、GOT、数据、链接数据一致。`__text` SHA-256 为 `5696e30d1be346de45a4e8fd245118e3645b5a94ea48360ab32e7b4ccf7d0d0d`。本地探针源码哈希也与部署 source_manifest 一致。

Wind 本体哈希 `c272d3ec569473ed0d9b7f79942546b263f9301046219b818497daee7c79b419` 与批准清单一致。因此更新完整签名文件哈希有已知代码等价依据，并非跳过 ABI 校验。

修改文件：`~/Library/Application Support/MachomeHub/data/redemption/abi-manifest.json`。修改了 probe_sha256 并新增核验说明。原文件备份：`~/Library/Application Support/MachomeHub/backups/wind-abi-20260911/abi-manifest.before.json`。

## 运行态验证与限制

14:23:42 观察到真实采集恢复：`monitoring=true`、`state=active`、Wind `subscribed`、错误为空。七标的 159513/159518/159561/159632/159660/159866/159941 均有新数据，数据时间为 14:23:38–14:23:40，订阅号为 393218–393224。

期间发现 UI 会在后台断开时自动启动 Agent，抢在 launchd 恢复之前取得实例锁。因此最后先退出 UI、完整温和停止旧 Agent 树，再启动 launchd，确认托管 PID 15618，最后打开 UI。helper 为 15621。该顺序避免后台由 UI 临时启动、launchd 因实例锁重试的问题。

**上述七标的成功是中间一次实测，不代表最后托管实例已经完成验收。** 最后一次启动后，helper 又在容器 chmod 权限检查中阻塞，尚需用户在系统设置中给予实际部署 App 完全磁盘访问，再检查数据持续推进。已停止重复重启，保留现有进程等待用户处理。

## 后续代码改造建议

### 用户完成完全磁盘访问授权后的复验（约 14:29–14:30）

- 系统 TCC 已有 `com.ellis.machome.operations-hub` / SystemPolicyAllFiles / auth_value=2。
- 用户点击系统要求的重启后，UI PID 为 15978（14:29:00 启动）；launchd Agent 仍为 15618，helper 仍为 15621，未再次重启后台。
- 后台实例 `56e9b8d8-619c-40ee-aaca-910aa4a9d7f7`，audit_ready、control_ready、client_config_bound 均 true，unresolved_modules 为空。
- 6787 health 返回 ok=true、monitoring=true；Wind subscribed，错误为空。七标的均收到 14:28:05–14:28:07 的数据，订阅 ID 393226–393232，原始回调 error_code=0，source_pid=15115。
- 相隔 15 秒的两次原始文件检查中，七个 callback_seq 均保持 1；确认了订阅与首批数据恢复，但观察期没有第二次推送，尚不能把健康状态当作持续变化数据或跨日无人值守的验收结果。

### 建议事项

- 发布时以最终签名后的探针文件生成/核验 ABI 清单，保留审批与内容等价依据；不能任意自动接受当前哈希。
- UI 自动启动 Agent 应服从 launchd 所有权，维护停止期间不抢启动；重启流程先关闭 UI 或显式禁止自启动。
- helper 的受保护目录操作与 LLDB 阶段需独立发布开始时间、进度和错误；当前主线程阻塞时 UI 仍显示“就绪/预热”，容易误判。
- 避免每次预热重复 chmod 已符合 0700 的目录；但这只是减少不必要的权限操作，不能替代容器读取/写入所需授权。
- 预热失败的按日标记和 pending 状态要有受控恢复路径。源码 requestWarmup 对同日已尝试直接返回；模块 stop/start 不完整重置这些标记，可能继续等待。增加错误分类、超时和人工重试路径，避免盲目循环弹窗。
- 权限修复后验证 launchd 托管、七标的数据时间和回调序号连续推进；单凭订阅命令回执或状态绿色不算恢复。
