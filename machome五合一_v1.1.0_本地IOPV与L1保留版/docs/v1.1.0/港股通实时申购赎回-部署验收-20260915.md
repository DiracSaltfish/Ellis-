# 港股通实时申购赎回：开发部署记录

2026-09-15，当前阶段：新版已部署，旧7只与新97只全部真实订阅并收到首帧；已恢复work模式且完成Wind退出清理。

## 已实现

- 原有7只池保持原接口和提醒；增加97只归档香港方向ETF候选的新池。95只与已有本地IOPV universe中的PCF验证清单交集，其余候选同样保留来源；这些证据不等同投资通道资格认定。
- 新池配置独立，去重并校验深圳1开头代码，两个池合计最多200只；无效新池配置不会使旧池消失。
- 原订阅先完成，再通过同一个helper逐只增量加入新池。新标的失败不撤销旧会话；未确认结果保留清理记录，并暂停后续新增。首次版本无无限自动重试，普通确定失败显示到对应标的，下次重新订阅周期重新尝试。
- 停订按最多8只分批执行，复用原探针ABI与SQL。旧Wind探针dylib未更换，批准的ABI清单未改动。
- 新池独立hk_history表，旧历史表结构不变。新池快照经权限0600的原子文件供本机网页服务读取，每秒更新。
- 原生申赎页新增页签，内网页面 /hk-connect-redemption，搜索/排序、时间、份额、篮子、比例、最新变化和异常状态。
- 篮子按当日PCF creation_redemption_unit换算；无有效单位则不显示篮子数。比例按当日累计份额，0:0、仅申购、仅赎回、跨日、过期均独立处理；提醒接口预留并强制关闭，未设5:1为实际触发规则。
- 新池订阅/解码/存储异常与旧提醒事件隔离；6787快照、名单、历史、PCF列表及单标的请求都不公开新池。

## 验证证据

- 首轮完整CTest 36/36；补充改动后申赎、helper、UI回归4/4；完整集成/原生验收在另一次6项回归中通过。
- Go全项目测试通过。
- 新增原生测试验证5:1计算、零分母、缺失与过期PCF、跨日、无效名单、部分订阅失败、旧接口隔离、停止快照及新池不触发旧弹窗事件。
- 原生离线截图和生产浏览器页面已检查，97只清单、158003搜索和无数据横杠正确；预览中的5:1数字是fixture，未注入生产。
- 部署包签名与配置验证通过；所需Qt均6.10.2，更新二进制链接使用现有包内Frameworks。

## 部署结果

生产路径：~/Applications/Machome 四合一运行中心.app。

构建ID：1.1.0-hk-connect-20260915T021814。

备份：~/Library/Application Support/MachomeHub/backups/hk-connect-20260915-021813（含原应用、配置、SQLite一致性备份、ABI清单及部署记录）。

增量替换Agent、UI、Wind helper和IOPV服务，保留其他已安装组件。配置仅增加redemption.settings.hk_connect_pool。新旧源文件映射及二进制哈希见 releases/hk-connect-20260915/manifest.json，部署脚本同目录deploy.py。

上线检查：旧7只、新97只，页面快照更新且feed_stale=false；IPC control_ready=true、client_config_bound=true、unresolved_modules=[]。

## macOS授权后的真实订阅

上线后Wind helper PID25494停在cleanupRawProbeCopies → QDir::entryInfoList → __open_nocancel，目标是Wind受保护容器目录。Wind本体已经退出，没有进行新的真实订阅。系统TCC中虽有同bundle ID完全磁盘访问条目，但绑定的是旧代码要求；新版读取其他App容器仍在等待授权。没有修改TCC数据库，也没有绕过系统权限。

用户已重新授权，helper完成容器清理，随后通过控制IPC进入临时weekend_test模式并启动Wind和监控。真实QMT下单门禁保持关闭。旧7只与新97只均收到真实首帧，97个新订阅ID互不重复；申购/赎回份额及笔数字段均为有限非负数，无订阅或解码错误。

网页展示的是Wind实际回传的累计值。用户确认当前为周一收盘值；由于探针提供的是本机回调时间，页面将时间列明确命名为“接收时间”，将顶部日期改为“系统日期”，不把凌晨回调时间当成证券数据交易日。休市期间无变化不会不断产生新回调。PCF未就绪只影响篮子换算，份额和比例不依赖PCF。

网页文案补丁仅替换Go服务，监督进程自动重启，Agent和Wind helper持续运行；备份目录下web-time-label-patch记录原二进制与补丁哈希。生产REST和WebSocket初始快照均验证仅7只旧池，新池未泄露到旧接口。

真实数据示例：159120.SZ申购2,000,000份、赎回4,000,000份，比例0.50:1。本次属于休市首帧验证，不代表交易时段持续变动验证。

## 最终验收

- 2026-09-15 02:31:30（上海）：旧7/7、新97/97均有真实首帧，Wind已订阅，feed_stale=false，提醒和新池外部接口关闭。证据：hk-connect-validation/live-validation.json、live-snapshot.json。
- 生产REST和WebSocket均仅返回旧7只，未附加hk_connect。
- 02:32:18 Wind退出清理完成，helper状态ready，无错误；02:32:38复查已恢复work、monitoring=false、全部订阅ID清空，网页仍显示97只已接收数据并注明停止状态。证据：hk-connect-validation/stopped-validation.json。
- Agent实例和PID在网页文案补丁前后未变化，IPC control_ready=true、client_config_bound=true、unresolved_modules=[]。
- 浏览器验证159120的2,000,000 / 4,000,000和0.50:1，以及158003的0 / 5,000,000与“仅赎回”；均来自真实Wind，不是fixture。
- 下个正常工作时段按现有日程自动恢复。当前篮子单位尚未确认，篮子列为—；不能以笔数字段替代篮子数。交易时段持续变化、后续拟定的提醒阈值不属于这次已完成的首帧验收。

## 2026-09-15 09:28 紧急停用

用户报告Wind卡顿，要求先停用新池，待验证修复后再启用。已确认全部停订，正常退出Wind并完成清理；生产配置hk_connect_pool.enabled=false，保留清单与历史备份，重启Agent加载配置，旧7只按原日程恢复。备份目录为hk-pool-disabled-20260915-092756。部署脚本改为保留已存在的enabled值，缺省关闭，避免后续部署意外重新启用。

探针SQL只选择etfbuynumber、etfbuyamount、etfbuymoney、etfsellnumber、etfsellamount、etfsellmoney，数据源ETFComprehensive.WholeETFData，LATENCY(500 MS)。没有选择价格/盘口字段；97个独立订阅和逐只探针注入的性能影响仍需离线/小规模验证，尚未认定卡顿根因。不得据此自动重新启用新池。
