# 侧栏与默认端口更新

正式网址为 http://192.168.1.113/ 。新增用户 LaunchAgent com.ellis.machome-iopv-web-entry，监听 80 并转发到既有 18680。原五合一内部 URL 及原入口保持兼容。SSE 即时转发，远程管理写入仍拒绝，未扩大管理权限。部署脚本 runtime-production/install-web-entry.py，程序 services/iopv/cmd/web-entry。

侧栏默认隐藏，左上按钮展开/收起并重绘图表，收起后腾出图表宽度；基金标题、代码、PCF 和日期/情景控件归入单一可横向滚动行。买1、卖1为浅黄色。无质量原因时提示条隐藏，有质量问题时保留。删除页脚和估值依据末尾说明。

正式应用更新前备份：machome ~/Library/Application Support/MachomeHub/backups/iopv-webfix-20260909-133236。只重启 IOPV helper，L1 ping/pong 验证通过，历史记录保留。
