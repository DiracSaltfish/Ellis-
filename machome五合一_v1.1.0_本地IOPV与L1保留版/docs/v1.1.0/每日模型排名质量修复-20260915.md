# 每日模型排名质量修复（2026-09-15）

已部署到 machome，生产复评结果 70/102 只，原快照 36/102 只。新增 1 只完整分钟复评、33 只覆盖 80%–95% 的参考评分；所有新增结果不通过严格初筛、不触发原提醒。原始行情、盘中快照和原 36 只分数保留。

## 诊断与规则

旧规则把最后成交时间超过 120 秒统一判为过期。新浪当日分钟成交记录证实部分港股长时间没有新成交。现在只有当来源、当日日期、正价格及响应接收时间均通过校验时，才允许旧成交时间用于评分；缺价、旧响应和无法证明的情况仍排除。新增轮询沿用批量请求，每轮最多探测 32 个冷门/缺价成分，失败后退避 60 秒。

历史复评采用只读 SQLite 和旁路核验记录，未向原历史补造价格。新浪分钟接口不含日期，必须以同日行情日期佐证；核验 1163 条基金分钟记录（48 只基金），期间无新成交才可复用原估值。完整评分仍要求 95% 覆盖；参考分最低 80%，保留至少 30 分钟、截点末分钟、PCF、汇率及份额等必要校验。页面可取消“包含复评/参考分”查看原结果。

32 只仍未评分：12 只没有有效资产估值，19 只覆盖或必要输入仍不足，1 只 TOPIX 不适用港股模型。新浪后续请求被限流；原生历史接口非强制登录返回 -98 DGW2，未踢出已有会话，因此未完成全量回补。空分钟响应不被认定为停牌。

## 验证

Go 全套测试通过；参考覆盖边界、旧成交/新响应判别、探测上限、重启恢复快照测试通过；真实数据库夹具验证 36→70，原 36 只评分和初筛结果不变。JavaScript 语法检查通过。生产 API 与浏览器验证 70/102，取消复评后为 36/102。修复了服务重启后排名空白，恢复结果明确显示为最近保存快照。

## 部署与回滚

服务器程序：`~/Applications/Machome 四合一运行中心.app/Contents/Helpers/machome-iopv-server`

SHA256：`9e32c0941c1365029a3a23c538263f5b016c78ca442dc5e82df0bee54c1277ec`

网页入口：`~/Library/Application Support/MachomeHub/web-entry/machome-iopv-web-entry`

SHA256：`c0ef2751d558b76afcc9a671f975468cb820fadb647a26342200eaef778134b3`

备份：`~/Library/Application Support/MachomeHub/backups/ranking-quality-20260915/{server,web-entry}`。回滚时恢复对应文件，重启网页 LaunchAgent 和 IOPV 子服务；无需重启 Wind。

核验旁路文件：数据库目录下 `ranking-no-trade-2026-09-15.jsonl` 与 `.manifest.json`。本地证据清单、复评结果、核验记录及生成脚本位于 `releases/ranking-quality-20260915`；生成脚本是本次审计用途，原始新浪响应位于 `/tmp/rank-audit-20260915`，不是自动定时任务。
