# 网页源码落盘核查

结论：本会话网页修改已落盘于新版本工程 services/iopv/web，不是只修改部署实例。

index.html、app.js、style.css、ledger.json 与正式 http://192.168.1.113/ 返回文件逐字节相等，四项 SHA-256 均匹配；完整哈希见 网页源码一致性.json。本地 releases/v1.1.0-production-layout 应用的 Go helper 与正式机相同，SHA256 dbbfcb6c9631f56a3260200cbb44d010a1afa719afe4c2a225400203c8f307e6。

源码重新执行 go build ./cmd/server 成功；通过 Go web.Handler 在内存 HTTP 响应中验证重新编译的四个嵌入文件与源码逐字节相同。index.html 按标准根路径 / 校验（文件服务会将 /index.html 重定向至 /）。JavaScript 语法及 chart-mode、chart-session、rollover 三个既有检查通过。

CMakeLists.txt 1019 起的 IOPV 构建依赖包含 web/*，覆盖新增 ledger.json；assets.go 的 go:embed 包含四文件，后续构建会包含这些网页改动。

本次只核查并保存报告，没有重启正式服务。旧版原始工程与独立模块不是此次新版网页的源目录，按此前隔离开发要求未同步覆盖它们。此报告确认磁盘保存与构建一致性，不等同于已提交 Git 或推送远程。
