# machome 界面更新部署验收

2026-09-07 已经用户授权，将 `ui-20260907-overview-zh` 安装到 machome 的 `/Users/ellis/Applications/Machome 四合一运行中心.app`。

此次使用仅更新界面的补丁。部署前核对线上应用全部 423 个普通文件与本地已验收 R4 基线相同；部署后只有四个文件变化：界面可执行文件、应用签名资源清单、界面版本记录、界面源码清单。后台构建仍为 `1.0.0-unversioned-dirty-20260907T033508Z`，原后台 build_manifest/source_manifest 保留，新增 ui_release/ui_source_manifest 明确记录界面版本，避免混淆两个构建。

## 验收结果

- 签名验证通过，新界面进程为 20247。系统窗口信息确认它处于活动状态、未隐藏，存在一个在屏幕上的正常窗口，尺寸 1480×952。
- 后台服务及包内任务的 16 个进程编号全部保留；后台实例仍为 `c3aba336-7ae1-4793-ac5f-c837ab5a04b6`。没有重启四个业务服务。
- 生产配置哈希未变；只读检查的配置绑定与控制面就绪均通过。
- 拉涨行情覆盖 198/198，行情源健康；Wind 已订阅，申赎监控正常。
- Webull 登录保留、接口在线，依旧暂无美股行情，未出现新的登录错误。
- 新浪、纳斯达克、标普、日经、德国五条上传任务的成功确认时间在界面更新后继续前进至约 12:33。
- 更新前已有的印度进程就绪、若干估值任务确认缺失/过期提示仍然存在。本轮界面更新不构成这些业务异常已修复的证明。
- 本次没有出现需要用户重新点按的业务权限阻断，未操作 DMIT、未调用交易接口。

远程 SSH 截图不可用，未为截图额外申请权限；窗口可见性由系统窗口列表与应用活动状态验证，本地界面视觉效果已在部署前完成截图验收。

## 回滚与证据

machome 完整备份：`/Users/ellis/Library/Application Support/MachomeHub/ui-update-20260907/before.app`。

同目录保留 `rollback-ui.py`，仅恢复本轮四个界面相关文件并重新打开界面，不替换后台程序。回滚脚本未执行。

- [部署回执](deployment/deployment-receipt.json)
- [部署前状态](deployment/before.json)
- [最终状态](deployment/final.json)
- [窗口可见性](deployment/window-check.json)
- [上传确认继续前进](deployment/upload-confirmation-check.json)

部署准备时修正了归档附带的 macOS 元数据和进程列表中的非法编码；界面启动改用当前用户会话的 `open -a`。这些处理未改变后台文件或服务进程。
