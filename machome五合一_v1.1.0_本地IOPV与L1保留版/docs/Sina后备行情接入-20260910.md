# 新浪后备行情接入（2026-09-10）

正式站点 http://192.168.1.113/ 已部署，源码与本地发布包已同步。

接口：HTTPS hq.sinajs.cn/?list=rt_hk09866,...，Referer 使用新浪财经页面。页面本身的 hqParser 定义最新价字段6、成交量12、日期17、时间18。只解析文本，绝不执行返回的 JavaScript。普通 hk 前缀实测为延时报价，不使用。

后备覆盖名单：00823、01698、09698、09866、09901、09961（均 .HK）。这些是本次验证的内网行情缺口，不能当作官方港股通资格名单。Config.sina_fallback_symbols 可覆盖，显式空数组禁用。

每次估值优先使用新鲜内网报价；仅名单内且内网无有效报价时使用新鲜新浪报价。源时间和接收时间均不超过120秒，必须为当日、价格为正且当日成交量为正。原内网订阅不移除，恢复有效报价后立即优先使用内网。后备单独缓存，避免覆盖内网数据；跨日清空。新浪失败或过期后不延长有效期。

工作日09:15–12:00、13:00–16:10批量采集，正常约10–20秒一次，HTTP超时8秒，失败退避上限60秒。沿用现有工作日判断，尚未引入完整港股节假日日历。零成交成分不会用昨收填为实时价。

Components API 输出 quote.source；分钟 Point 保存 fallback_components；网页估值依据显示后备来源。原L1对外转发和五档数据不受影响，不伪造新浪五档。

验收：go test ./... 与 go test -race ./internal/live 通过。正式站点新浪6只报价可用，12只基金使用后备；520920恢复30/30，中间价IOPV约0.641969（11:00瞬时值）。余下缺价00853、02252、02172、01475均为零成交/陈旧报价，本次未作为实时价补入。并未仅凭这些现象确认停牌。

回滚备份（machome）：~/Library/Application Support/MachomeHub/backups/sina-20260910-110017。此次无数据库结构迁移；仅替换估值helper，保留其他进程。部署脚本 runtime-production/deploy-sina.py。
