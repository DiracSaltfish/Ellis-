> 本页为清理前检查记录；授权清理已完成，最新结果见 [清理完成记录](清理完成.md)。

# 本地磁盘占用与安全清理清单

范围：`/Users/ellis/工具程序开发/machome四合一运行改造cpp`。2026-09-08 只读检查，尚未删除任何文件。

## 结论

- `du -sk`：8,886,328 KiB，约 8.475 GiB / 9.100 GB。
- 11 个主要 CMake 构建目录合计约 1.787 GiB；13 个发布目录合计约 6.569 GiB，其中包含 8 个 archive 目录、31 份历史归档，共 4.341 GiB。
- 发布包内重复包含 Qt 框架、辅助程序和 Upload 运行时，同一发布目录通常同时保留 .app 和 .zip；多套构建目录重复保留目标文件、静态库和测试程序。
- `app/scripts/build-release.sh:365` 每次把旧产物移入时间戳 archive，没有归档数量/期限上限。使用 dist-candidate、dist-live-readonly-r2/r3/r4 等新目录又保留了额外副本。

## 安全核查

- 所列主要 build/dist 目录内 Git 已跟踪文件数均为 0。源码的未提交 UI 修改和新增 UiWidgets 文件不在清理范围内。
- 未发现本地运行进程、可读的 LaunchAgent/LaunchDaemon 配置或 lsof 可见打开文件引用目标目录；这是检查时的状态，实际删除前应再核对。
- 这些发布/构建目录的文件名扫描未发现数据库或 .env 文件；扫描发现的 PEM 为包内 CA 证书束，不是单独业务数据。
- machome 当前应用位于 `~/Applications/Machome 四合一运行中心.app`，不是从本地源码目录运行。当前版本为 `1.0.0-97e5b63312b8-dirty-20260908T005024Z`。
- machome 旧版回滚副本 `~/Library/Application Support/MachomeHub/ui-update-20260908/before.app` 已确认存在。检查没有停止任何服务。

## A：可重新生成的编译缓存

删除下列目录会失去增量编译缓存和部分原始测试日志；源码和构建脚本保留，下次需要重新编译。

| 相对目录 | MiB |
|---|---:|
| `app/build-integration-root` | 327.77 |
| `build-monitor-sync-release` | 283.08 |
| `app/build-release-integration-root` | 206.45 |
| `app/build-acceptance-20260905` | 188.32 |
| `build-release-native` | 153.20 |
| `app/build-release` | 150.11 |
| `build-cpp-default` | 146.94 |
| `build-cpp-native-334` | 145.35 |
| `app/build` | 139.83 |
| `app/build-release.pre-schedule-audit-20260904T1705` | 45.64 |
| `build-cpp-native` | 43.47 |

另外：`prototype/build` 约 1 MiB；`app/components/upload/business/frontend/node_modules` 约 61 MiB，可由锁文件和 npm ci 重建；`__pycache__`、`.pytest_cache` 是小体积缓存。`app/components/upload/machome-upload-web` 约 13.6 MiB 为生成的 Go 可执行文件，可重建，但对总占用影响很小，不必优先处理。

## B：历史发布归档

建议先固定保存最新已部署版本的 ZIP、校验值及一个回滚版本，再清理下列目录。删除后不能直接使用这些特定历史二进制回滚；历史 dirty 版本未必能仅凭 Git 完全重建。

| 相对目录 | MiB | 归档份数 |
|---|---:|---:|
| `dist-cpp/archive` | 1748.62 | 13 |
| `app/dist-integration-root/archive` | 821.21 | 6 |
| `dist-native/archive` | 536.77 | 4 |
| `app/dist/archive` | 500.57 | 4 |
| `app/dist-candidate-20260905/archive` | 218.23 | 1 |
| `app/dist-live-readonly-20260907-r3/archive` | 206.47 | 1 |
| `app/dist-live-readonly-20260907/archive` | 206.42 | 1 |
| `app/dist-candidate-20260907/archive` | 206.41 | 1 |

A + B + node_modules 预计清理约 6.64 GB（约 6.19 GiB）。这是按文件占用统计，APFS 克隆/快照可能影响实际可用空间增量。

## C：进一步合并候选发布包

以下是发布目录总量，已经包含上面 B 的 archive，不能重复相加：

| 相对目录 | MiB |
|---|---:|
| `dist-cpp` | 1882.80 |
| `app/dist-integration-root` | 958.18 |
| `dist-native` | 671.02 |
| `app/dist` | 634.86 |
| `app/dist-candidate-20260905` | 424.30 |
| `app/dist-live-readonly-20260907-r3` | 412.94 |
| `app/dist-live-readonly-20260907` | 412.84 |
| `app/dist-candidate-20260907` | 412.82 |
| `dist-monitor-sync` | 206.89 |
| `app/dist-live-readonly-20260907-r4` | 206.47 |
| `app/dist-live-readonly-20260907-r2` | 206.45 |
| `app/dist-pages-20260907-bebaa87` | 149.54 |
| `app/dist-ui-20260907` | 147.84 |

这些目录中最新的带版本清单包仍是 09-07 版 `dist-monitor-sync`。建议暂保留它作为本地旧版回滚，其他旧 dist 目录在确认无需特定历史包后可合并清理。连同 A 共约 8.75 GB；此数是替代 A+B 方案的总量，不再叠加。

## 最新部署包应持久保存

- 本地最新 ZIP 在 `/tmp/machome-ui-release-clean-20260908/Machome-Operations-Hub-macOS.zip`，约 58.95 MiB。它不在本次统计目录中，且 /tmp 不适合作为长期唯一存放位置。
- SHA-256：`fe6240beb0e19ffbacedf4b9335a06cb67224838f8bc6247a02cd85b4397ed87`，本次已重新计算核对。
- 真正执行历史包清理前，先把该 ZIP、SHA 和 build/source manifest 固定保存到例如 `releases/current/`，另保留一个已知旧版回滚包。当前检查尚未复制或清理它们。

## 必须保留

`app/src`、`app/modules`、`app/helpers`、`app/components/upload/business` 的源码、`app/third_party`、`config`、`contracts`、`migration`、`plans`、构建脚本与锁文件，以及当前未提交修改。`docs` 总共约 35 MiB，包含验收、迁移和部署证据，不是主要占用来源，建议保留。不要对整个项目使用 git clean -fdx。

## 防止再次膨胀

统一一个日常构建目录、一个发布目录；发布策略保留当前版本 + 上一个可回滚版本，较早包转移到用户指定归档位置或按规则删除。保留 ZIP + 校验值 + 版本清单通常足够，不必在每个候选目录长期保留一份解压后的 .app。
