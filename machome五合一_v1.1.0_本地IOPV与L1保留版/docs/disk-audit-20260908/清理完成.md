# 清理完成记录

2026-09-08 09:15，按用户授权删除 25 个已核对的本地编译/发布目录（含历史 archive、候选安装包及 prototype/build）。

- 旧产物删除量：8.974 GB。
- 清理前项目约 9.10 GB；保留两份部署 ZIP 后，清理完成约 249.5 MB，净减少约 8.85 GB。
- 当前部署版本已固定保存到 `releases/current/`，上一版回滚包保存到 `releases/previous/`。两份 ZIP 都经过 CRC 和 SHA-256 校验。
- 删除前核查：目标目录无 Git 跟踪文件、无嵌套仓库，未发现运行进程、打开文件或启动项引用。
- 删除前后 2,073 个保留文件哈希完全一致；其中已部署版本清单涉及的 470 个源码文件逐项校验通过。源码、未提交修改、配置、文档、依赖源码保留。
- 没有清理 node_modules、没有清理 /tmp 下其他构建目录、没有操作 machome 或 DMIT。
- 没有执行全局 git clean，也没有重新编译以免再次生成刚清理的缓存。

逐项删除清单和实际字节数见 `cleanup-receipt.json`。`清理建议.md` 和 `directory-sizes.json` 保留为清理前的检查记录。
