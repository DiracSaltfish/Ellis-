# 验证记录（2026-09-09）

## 独立服务最终联调

现已完成常驻服务、真实TGW、分钟SQLite、REST/SSE、网页和macOS管理窗口。以下是本轮最终运行证据；后面的P0条目保留为早期计算原型记录。

- Go测试含子用例/Example：**158项通过，0失败**；竞态检查、go vet、前端JavaScript语法检查通过。原生C++ CTest 1/1通过。
- 真实TGW收到14/16两类full/delta，未使用模拟行情。120份当日PCF，750订阅；缺失成分不补伪造值。
- SSE两条连续事件的接收帧数实际增长。管理暂停/恢复、SQLite quick_check通过。
- 重启后run_id改变，重启前513090的历史分钟仍可读取。
- 联调时独立Decimal复核61只当时完整篮子的中间价/两方向结算公式，与API输出差异<1e-12元/份。
- 数据库当前记录1680行，分钟范围10:48–11:01；之后服务继续采集。
- 本机及本机内网地址的网页HTTP200；原生管理窗口进程已启动。未把代码/HTTP验收描述为截图或整日视觉验收。
- 原始证据：outputs/live-acceptance.json、final-tests.jsonl、service-race.txt、final-ctest.txt。

## P0历史记录


## 已运行

| 验证 | 结果 |
|---|---|
| `go test -json ./...` | 148 个 test/subtest/example 通过，0 失败；原始日志 outputs/tests.jsonl |
| `go test -race ./...` | 通过；仅覆盖 Go 层检测能力，不代替 TGW 多线程长稳测试 |
| `go vet ./...` | 通过 |
| CMake Release + CTest | 原生 C++ 静态库构建成功，C ABI 测试 1/1 通过；见 outputs/ctest.txt |
| `go run ./cmd/replay` | 120 只全部复现原历史审计值，最大绝对差 2.220446049250313e-16 元/份 |
| 100成分 C++ 经 cgo 微基准 | Apple M4 / darwin arm64，453.4 ns/op，3232 B/op，2 allocs/op；不包含网络和落盘 |

覆盖公式、负预估现金、人民币现金不换汇、必须替代现金、非法/NaN/Inf/溢出输入、HK代码补零与去重、PCF日期身份/条数/字段、159900排除、未知旗标拒绝、历史120篮子、750订阅、3组节拍跳帧、双通道双方向FX映射、FX日期/陈旧状态、QC未准入、成分缺价/过期/未来时刻、午休/收盘、分钟取样期限、五档溢价、HTTP403/伪HTML/错日响应。

首次回归暴露 PCF 代码 `1` / `00001` 等格式不一致，已在进入缓存和订阅规划前标准化为5位港股代码，并增加防重复测试。未把这个失败简单当作缺行情跳过。

## 实际外部只读探针

- 2026-09-09 513090.SH：上交所 XML，17 只成分，500000 份，预估现金 9331.24 元；SHA-256 `cb52738d22c12633784377d7d946510148757588f8bf339c1de94e3ad3cad2da`。
- 2026-09-09 159217.SZ：深交所 XML，排除现金占位后36只成分，1000000份，预估现金 −402.84元；SHA-256 `715de05d4011fb70a5b25f59c745454f187acc39ab8147363f6856e6c6d30a88`。
- 1navs 汇率 API HTTP200，schema=hk-connect-fx.v7；快照 generated_at=2026-09-09T10:19:38.150646791+08:00，源报价时刻10:17:57。中间价0.86418；沪/深买卖预测分别解析。fixture 作为当时快照测试，超3分钟必须拒绝实时使用。
- 1navs 两个 private 网页由 web 抓取器打开失败；通过实际JSON API及本地Go/Vue代码与部署说明理解接口，不把未成功抓到的页面作为已验证页面。

PCF保存在 `outputs/pcf/2026-09-09/`，FX夹具在 `testdata/fx_20260909.json`。

## 证据界限

120 个候选的全部PCF都从既有审计目录复制并带hash。历史中间价回放复用既有不复权收盘价，与此前审计数值一致是实现回归，不是新的双行情源独立验证。120 中22只的旧收盘价问题原样保留；没有把 price_date 改成当日来绕过实时门禁。

2026-09-08 未取得本轮可审计的逐分钟预估结算汇率序列，因此未伪造三线历史成图、未声称结算模型完成历史回归。上图三线/下图双溢价、09:30–16:08的布局和空值规则已写入实施方案，并通过分钟逻辑测试。完整网页还未实现或浏览器验收。

本轮没有运行真实TGW订阅、没有部署服务、没有触发订单或申赎、没有改动五合一/newnavnav源码。生产日历、并发报价缓存、价格缩放实测、读写API、SSE、SQLite常驻writer、网页及五合一集成是后续工作，不计入本轮通过数。
