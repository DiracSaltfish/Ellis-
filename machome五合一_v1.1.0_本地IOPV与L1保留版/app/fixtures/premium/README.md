# Premium A replay fixtures

These records are synthetic and contain no account, token, host, or production
credential. The golden full snapshot fixture exercises the exact integer
mapping and summary contract inherited from the frozen A-side baseline.

`watchlist.json` is a synthetic mixed-market list used by the bundled TGW
helper lifecycle smoke test.
