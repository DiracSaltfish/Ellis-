# 2026-08-27 v0.2.0 部署、联合调试与上线意见

## 发布身份

- 源码提交：`754139d4925d05fd9cae2822ae5d14c9f1d2c5d7`
- Git 标签：`v0.2.0-hkt-hotlist-20260827`
- macOS arm64 包：`dist/ETF溢价率拉升监控-macOS-arm64.zip`
- 发布包 SHA-256：`303b639cc9829c83c8f46ab2713b94b49772f0e66ca381403a11e5fe3703661c`
- Core SHA-256：`7fad07b253de1343246dc53ccc39a348a36c36f0e0056ab70f8bfb3eff553f6f`
- Adapter SHA-256：`50c1b9e4c05609a9a9f82b37462dbccdf3b94d7e6ca2fde7de3eaee3d8f51919`

两个 app 和 core 均为 Apple arm64、版本 0.2.0，并通过 `codesign --verify --deep --strict`。`macdeployqt` 扫描 Homebrew 的非本项目可选插件时报告找不到 QtPdf/QtSvg/QtVirtualKeyboard 的候选 rpath；最终 bundle 的实际内容全部通过深度签名校验，本机 B 也已从该 bundle 成功启动并长连，因此这些扫描信息不构成当前运行时缺库。

## 备份与 Git 关键节点

- 改造前关键节点：commit `d33433f06446231ce24b512b777d2f662cb6bbc6`，tag `critical-pre-hkt-hotlist-20260827`。
- 改造前本机/远端 dist：`/Users/ellis/Backups/ETF溢价率拉升监控/critical-node-20260827-161123/`。
- 最终覆盖前 machome 静态安装：`/Users/ellis/Backups/ETF溢价率拉升监控/pre-final-754139d-20260827-1748/`，包含 282 项 SHA-256 清单。
- 功能提交 `22ff89d` 和审查加固提交 `754139d` 均已推送 `origin/main`。

## machome 部署结果

安装目录：`/Users/ellis/Applications/ETF溢价率拉升监控`。

部署只停止和重启了本系统的 core、adapter、console。以下既有服务全程保留：

- ETF 申购赎回监控相关服务仍监听 `*:8765`；
- Webull 行情转发器仍监听 `*:18765`；
- 新 A-core 监听 `*:8421` 和 `*:19195`。

远端私有 `app.json`、TGW 账户文件、项目 `.venv`、日志、数据、runtime 和运行时观察清单均原位保留。运行时观察清单是 203 个标的：原 202 清单加用户现有的 `159518.SZ`，部署没有擅自删回 202。额外 L1 热维护表已由 Core 原子保存为 `02800.HK`；总 pinned union 为 204，上游仍按证券去重为单流。

桌面入口保持：

- `/Users/ellis/Desktop/ETF溢价率拉升监控服务端A的替身`
- `/Users/ellis/Desktop/ETF溢价率拉升监控客户端B的替身`

二者仍分别指向安装目录内的 0.2.0 console/client app。

## 本机 B 与 machome A 联合调试

本机发布版 B 使用 `--read-only` 和独立临时设置启动，未连接或发送任何真实 QMT 写请求。

### B 进程

- 连续 60 秒保持 `192.168.1.23 -> 192.168.1.113:8421` 为 `ESTABLISHED`；
- 10 秒和 60 秒检查 CPU 均为 0.0%，RSS 约 162.6 MiB；
- 无崩溃、无重连、日志无运行时错误。

### 8421 summary/detail

120.020 秒双 WebSocket 探针结果：

| 指标 | 结果 |
|---|---:|
| 断线 | 0 |
| ping | 24 次 |
| RTT min / p50 / p95 / max | 3.338 / 5.048 / 9.447 / 13.429 ms |
| summary 总流量 | 20,488 B |
| summary 平均流量 | 170.71 B/s |
| sync 延迟 | 0.057 ms |
| status | 24 条 |
| detail ack | 1 条，59 B |
| adapter gap / SDK queue | 0 / 0 |

测试发生在 16:00 之后，状态为“盘外驻留”、active upstream=0。详情连接立即返回订阅 ack，但没有缓存首帧；这是重启/session 失效后禁止发送旧快照和港股 16:00 停订的正确行为，不能解释成详情失败，也不能据此宣称盘中首帧延迟已经通过。

### 19195

- hello 中 monitor defaults 为 203；
- 同一请求中的 `159866.SZ` 和 `02800.HK` 均被接受；
- 盘外两者都返回 `missing`，没有发旧 session 缓存；
- status 显示 hot=`["02800.HK"]`、market_online=false；
- 部署后 10 分钟内 data 目录没有新文件，符合盘外无行情和 hot-only 不落盘边界。

## 测试和审查总览

- Debug、Release、`-Wall -Wextra -Wpedantic -Werror`：各 CTest 5/5；
- Debug CTest 额外连续 5 轮全部通过；
- ASan + UBSan：5/5；
- Python 正式矩阵：15/15，ResourceWarning 视为错误；
- Python `compileall`：通过；
- GPT-5.5 XHigh 独立静态审查累计提出 4 个 P1，全部修复；最终复核 P0=0、P1=0；
- 全部自动化、联合调试均未发送真实申购、赎回、买入、卖出或撤单请求。

## 实际上线意见

结论分层：

1. **A 股观察、B 端、8421 与原 19195 沪深兼容链路：建议上线运行。** 代码、协议、生命周期、静态审查、打包和局域网长连均已达到当前发布门槛；现有申购赎回监控与 Webull 服务未受影响。
2. **额外 L1 热维护的管理、去重、内存/持久化隔离：建议上线运行。** `02800.HK` 已保存，盘外行为正确；旧 CPPUI 只请求 `.SH/.SZ`，v1 结构没有变化。
3. **深港通 HKT 实时数据：维持“生产候选、条件上线”，暂不标记全面验收通过。** 2026-08-27 尾盘已经证明 `02800 + kSZSE + kHKTSnapshot` 能登录、订阅、收 full/delta 和退订；但当前最终 0.2.0 部署发生在 16:00 后，仍需下一交易日 09:30–16:00 完成多证券、独立行情源、重连、午间、15:00/16:00 时段和长连验收。
4. **真实 QMT 委托：只允许用户本人从 B 界面操作。** 本次没有验证真实券商回报、部分成交或超时后的人工重试，因此不对真实交易结果作自动上线背书。

下一交易日若 HKT 盘中出现字段隔离、持续退避、adapter gap、SDK 队列积压或 19195 首帧异常，应只将 HKT 热维护部分降级为未通过，不必回滚已验证的沪深监控与 v1 服务；所有异常需保留原始证据后再决定是否扩大港股标的范围。
