# 13｜2026-08-27 盘中生产联调记录

## 结论边界

2026-08-27 11:28–11:29，最新代码已部署到 `ssh machome` 的生产路径并接入真实 TGW。A-console、A-core、tgw-adapter 和只读 B 均成功启动；8421、19195 监听正常。此次窗口临近午休，只证明短时真实行情、沪深追加/移除、A→B/旧 L1 协议和三标的 Sina 对照，不替代 202/500/1000 各 15 分钟、全日、断线重连及独立 IOPV 验收。

全程以 B `--read-only` 运行；未向真实 QMT 发送申购、赎回、卖出或撤单。

午间按用户后续指令，B 默认启动策略改为开放人工交易，并新增持久化设置页；`--read-only` 仅作为紧急开关保留。该代码变更的自动测试和界面检查仍未发送任何真实 QMT 写请求，真实申赎/卖出/撤单只交由用户本人操作。

## 部署与运行证据

- 11:28:14 adapter 登录 TGW；202 标的按 20 个一批全部 `subscribe_accepted`。
- A-core PID 93103，监听 `*:8421`、`*:19195`；adapter PID 93105。
- B 首次进程被关闭但无 macOS crash report；11:30 后重新拉起 PID 93288，午休空闲 CPU 0.0%，状态 `S`。
- 原观察清单 SHA-256 `c1b1c195a89f9883fb895a2823dd8f1de81d9ca1388fcba11bac8b74e7de4942`，运行时追加/恢复后哈希不变。

## 真实快照

### 159866.SZ 日经 ETF

11:28:48 附近：

| 字段 | TGW/A | Sina L1 |
| --- | ---: | ---: |
| 最新 | 1.687 | 1.687 |
| 今开/最高/最低/昨收 | 1.675 / 1.689 / 1.674 / 1.680 | 相同 |
| 买一至买五 | 1.686 / 1.685 / 1.684 / 1.683 / 1.682 | 相同 |
| 买量一至五 | 703,200 / 475,600 / 404,200 / 891,700 / 1,313,400 | 相同 |
| 卖一至卖五 | 1.687 / 1.688 / 1.689 / 1.690 / 1.691 | 相同 |
| 卖量一至五 | 604,200 / 1,594,800 / 913,800 / 2,244,000 / 637,400 | 相同 |

8421/19195 冒烟同时返回：`legacy_lp=1.687`、`summary_price_e6=1687000`、detail 10 档、raw `adapter_seq=539`。

### 164824.SZ 印度基金 LOF

11:29:03 A summary：`last_price_e6=1313000`、`bid1_price_e6=1312000`、`iopv_e6=0`、`change_ppm=-3037`，质量位只有 `iopv_non_positive`。Sina L1 最新/买一/卖一为 1.313/1.312/1.313，五档价格和数量与 TGW/A 一致。

结论：LOF 实时 L1 可以正常订阅、解析和分发；无 IOPV 时不计算溢价，但可在完整 150/300 秒窗口后参加买一拉涨模型。

### 164701.SZ 黄金 LOF

11:28:48 附近：最新 1.793，买一至买五 1.792/1.791/1.790/1.789/1.788，卖一至卖五 1.793/1.794/1.795/1.796/1.797；TGW/A 与 Sina L1 相同。

### 513100.SH

11:29:05 A summary：`last_price_e6=2231000`、`bid1_price_e6=2229000`、`iopv_e6=1976000`、`sell_premium_ppm=128036`、quality 为空。该值证明上海标的及 IOPV 字段已进入核心，但 Sina L1 不提供独立 IOPV，因此不能据此宣称 IOPV 真值已验证。

## Sina 统计对照

`logs/live-validation/sina-compare-20260827-1130.json`：

- 66 个价格值 TGW/Sina 比值 min、p50、p95、p99、max 全为 1.0；
- 60 个五档价格 exact rate 1.0；
- 60 个五档数量 exact rate 1.0，数量比例 min/p50/p95/p99/max 全为 1.0；
- TGW/Sina 盘口结构错误均为 0；reader 和 Sina poll error 均为空。

## 运行时观察清单

通过 loopback 8421 将 202 临时改为 204，追加 `164824.SZ` 和 `513100.SH`；两者均取得 full。恢复 202 时：

- B 收到两个 `symbol_removed`；
- 2 秒安静期两标的均无 summary 再发布；
- A 日志记录 `subscribe_accepted`、`unsubscribe_accepted`；
- 原 202 清单原子恢复且哈希不变。

当时 19195 对 `164701.SZ` 尚处于 60 秒动态订阅宽限，故上游 maintained 短暂为 203，属于预期引用计数行为。

## 价格精度与 B 响应

- TGW 场内价格保留 e6 整数，非零报价必须 `% 1000 == 0`；本次生产日志 `price_tick_0_001_invalid=0`。
- B 最新价、十档和缓存卖出价显示三位；IOPV 显示四位。
- 本地彩虹圈复现时旧 B 持续 100% CPU。堆栈指向 `ResizeToContents → sizeHintForColumn`，不是业务永久循环。
- 修复后普通行情不排序，信号才置顶；摘要 100ms 合并，信号即时；同一 202 仿真负载约 2% CPU且界面状态可立即读取。

## 下午/后续待办

1. 13:00 自动清空窗口，13:01 开信号；150 秒盘口分支最早 13:02:30，300 秒分支最早 13:05。
2. 连续观察 B 进程、声音/弹窗、信号置顶和 A/B 延迟；保持 `--read-only`，除非用户明确切换交易模式。
3. 按计划完成 202、500、1000 各至少 15 分钟容量段和断线恢复。
4. IOPV 继续标记“字段映射可用、业务真值待独立验证”。

## 午间局域网复验

开发机 `192.168.1.23` 到 machome `192.168.1.113` 的 8421/19195 TCP 和协议冒烟均通过。8421 summary/detail 双连接保持 300.014 秒，断线 0、ping/pong 60/60，RTT p50 6.277 ms、p95 85.185 ms；详情缓存首帧 8.600 ms，午休仍收到 5 次后续主动快照。午休持续 summary 约 2.97 KB/s；真实盘中按 31.575 条/s 和实测 summary 大小估算约 19.5 KB/s/客户端，当前无需为少量 B 优化网络协议。

最新版 B 终端直启并强制 `--read-only` 后可以显示远端 202 标的，未出现彩虹圈；但 Finder/LaunchServices 启动被 macOS 局域网隐私策略阻断。Info.plist 缺少局域网用途说明且 build bundle 未完整绑定签名，现有 `dist` 又是早于最新设置/交易开关的旧包。完整证据和打包复验条件见 `docs/14_20260827_局域网与详情长连验证.md`。

12:17 后续已按当前源码重新生成 Debug、Release、`dist/macos-arm64` 和 zip，补齐局域网用途声明与完整 bundle 签名；新 B 已识别 `--settings`/`--read-only` 并只读连通 A。完整重编结果见 `docs/15_20260827_全量重编译与发布记录.md`。
