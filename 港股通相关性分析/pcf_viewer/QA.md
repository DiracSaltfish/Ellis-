# 验收记录 · 2026-09-08

- 本机真实数据解析通过：520600，390 日期、69 历史代码、16,392 明细行；规范化后未发现重复主键。
- Python 8 项测试通过：代码归一化、空值与零、相邻差值、别名、非法值/日期、缓存刷新、CSV 筛选、HTTP 只读路由。JavaScript 语法检查通过。
- 浏览器实际验证：代码搜索/切券、近30日（30条日期）、手动日期范围、滚轮缩放、主图拖动平移、时间轴左手柄缩放、选区平移、变化日过滤、CSV下载事件。
- 样例核对：09660，2026-06-12 为 2,511，06-15 为 3,910（+1,399；+55.7149%），06-16 为 3,915（+5）。
- 退出样例：02362 在 2025-08-11 显示未列入/退出记录，不计算负数量；最新30日无值时显示空区间提示，历史仍可打开。
- 1280px 桌面及 390px 窄屏截图检查通过，窄屏 document scrollWidth = clientWidth = 390，无全页横向溢出；已恢复默认浏览器尺寸。
- 最終 CSV 改为同源附件响应，内置浏览器下载事件已验证；无外部 CDN 或网络字体依赖。
- 通过本机内网地址 http://192.168.1.23:8766/health 检查，服务监听所有接口；未从另一台物理设备验证防火墙通行。
- 当前后台进程 PID 12124（仅本次启动有效），日志为同目录 server.log。未安装开机自启；重启电脑后用 start.command 启动。

范围限制：未同步新 PCF；未核实 CSV 缺失的整个交易日；图只反映原始单篮股数，不做篮子单位变更归一化、复权或指数调整原因推断。历史证券更换交易代码时分开显示，不自动合并同名代码。
