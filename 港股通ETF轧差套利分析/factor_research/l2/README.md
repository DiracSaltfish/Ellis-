# L2研究

当前已完成：2026年已保留L2数据的C++20核心、pybind11、14:30/14:45截断批量计算及首次方向与数量历史外推检验。

本次结果见 [方向与数量回测报告](study/L2方向与数量回测报告.md)。170只ETF、24日、3,481基金日；测试只有4日，尚不能认定稳定90%命中，数量误差仍较大。

- `L2识别与C++撮合方案.md`：具体方案、沪深状态规则、因子、验证与性能设计。
- `native/README.md`：已实现内核、CLI、Python扩展、批量入口、测试命令和适用范围；`native/validation/`保存真实数据独立核对回执。
- `extract_2026.py`：按六位目标代码提取2026数据，兼容有/无日期目录层；CRC核验后保留规范市场目录，原7z保留。
- `build_l2_features.py`：Python机制核对原型。不是最终生产引擎；目前交易时段配置仅供2026年1月等适用旧规则的样本，不用于全年批量结论。
- `study/`：本次正式冻结样本、日期外推、概率与数量模型、测试预测明细及案例图；`analyze_l2.py`保留为早期原型。
- `results/`、`valuation/`等早期2025探索产物不代表完整沪深样本，不用于本次2026结论。

源数据在machome的`/Volumes/EllisFiles/Stocksdata/A股逐笔`。禁止修改或删除该目录以外的行情源数据。

2025年后缀误识别事件：原按后缀筛选漏掉部分上海ETF并清理了22个原压缩包，1月20日可从用户已解压目录找回；用户明确要求2025年不回补。保留` suffix_incident.json `作为审计记录。2026流程已改为按六位代码及PCF/网站映射确认市场，禁用旧脚本的压缩包删除路径。

## 2026空间清理（用户2026-09-12明确要求继续处理并释放空间）

`extract_2026.py --cleanup`先处理已核验存档，然后逐个解压完整下载的2026包。每包提取后重新读取原压缩包目录，确认按六位代码匹配的完整沪深目标集合与清单完全一致，重新校验所有保留文件的CRC和大小，确认原包在处理期间未改变，才删除原7z。正在下载的文件不处理。只删除指定A股逐笔目录内的原包，不使用递归删除。

`cleanup_2026.py`可独立复核并清理已有manifest对应的2026原包。旧`extract_archives.py`删除功能仍禁用。

### 外置盘满时的应急处理

`emergency_space_2026.py`只处理一个完整2026原包：先在machome内置盘的本研究`emergency_recovery`目录提取完整沪深目标集合，逐文件验证大小与CRC并保存恢复清单，然后删除指定A股逐笔目录内的该原包。腾出空间后将目标文件复制回规范保留目录并再次校验。内置盘恢复副本保留，不删除原始行情目录外的文件。随后可继续普通`extract_2026.py --cleanup`流程。
