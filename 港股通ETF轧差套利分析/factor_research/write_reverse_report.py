from pathlib import Path
import csv,json
R=Path(__file__).resolve().parent;D=R/'reverse';O=D/'results'
def read(n):return list(csv.DictReader((O/n).open()))
def pct(v):return f'{float(v)*100:.1f}%'
factors={r['feature']:r for r in read('reverse_factor_evidence.csv')};fresh={r['feature']:r for r in read('fresh_factor_direction.csv')}
cohorts=read('reverse_cohorts.csv');models=read('forward_models.csv');rules=read('rules.csv');spec=json.loads((O/'frozen_spec.json').read_text())
s='''# 从实际净申赎反查路径，再正向验证因子

研究日期：2026-09-12。延续全天有效分钟、当日最终汇率固定的研究口径。数据和计算位于machome同名研究目录；本机保留完整报告、代码、特征表、图表、评分和固定模型。

**结论：有可重复的共性，最强的是持续的结算溢价、成交额发生时的溢价状态，以及历史净流量延续性。** 单纯放量、篮子涨跌或一段尖峰回落，远不如这些组合稳定。将新共性加入模型后，方向判断有小幅改善，但相对之前已有的完整因子模型，尚未证明净申购识别有显著增益。数量预测仍不足以精确还原净增篮子。

本轮另外重建了此前未分析的2026年7月至8月3日：3,830个基金日、182只基金、22个有效交易日。其中净申购占16.1%，大量净申购占6.1%，大量净赎回占9.1%。8月仅含8月3日，不把它当作完整月份。

## 1. 如何从结果往回看，如何避免只解释历史

标签直接使用[1navs历史份额](https://1navs.com/funds/SZ159570/share-history)。日期T的净变化直接对应T日，不平移。全部196个候选标的的历史响应此前已缓存；本轮又在线抽核513120、159015、159570，分别比较266、62、272个日期，没有发现与缓存不同的份额值。

用2025年7—12月的15,185个基金日做“发现样本”，按最终结果分五组：

- 大量净申购：净增≥10篮子，且≥昨日份额0.5%。
- 大量净赎回：净减≥10篮子，且≥昨日份额0.5%。
- 其余正净量、负净量分别是小额净申购、小额净赎回。
- 绝对净量≤0.01篮子视为份额不变，避免份额四舍五入噪声。

先比较五组的逐分钟溢价、篮子涨跌、成交额分布，再提出候选因子。候选因子是否保留，只看发现样本：与净份额变化的Spearman相关绝对值≥0.08，且六个月里至少三分之二方向一致。还检查同一基金内部的相关，以及各交易日内的横向相关，降低基金规模和市场共同波动的干扰。这些检查不是因果识别。

正向检验分三层：

1. 2026年1—3月只用来选择简单规则。2026年1—6月还做逐月前推：过去数据拟合、上月评分定阈值、当月检验。这些月份此前已经分析过，明确作为重复使用的历史检验。
2. 新验证模型统一只训练到2026年5月；6月模型评分的90%分位数固定为阈值。
3. 所有特征、规则、模型参数固定后，才计算7月至8月3日的表现。新验证段不调参数、不挑最好阈值。

全部模型都使用同一套固定小型梯度树参数，没有按新验证结果选择算法。最终模型保存了各模型组和三个目标的版本，详见 `frozen_spec.json` 和 `frozen_models.joblib`。

## 2. 反向看见了什么共性

![五组的全天路径](results/reverse_paths.png)

图中每条线是逐分钟的组内中位数，不是某只基金的模拟交易路径。明显的区别来自**溢价水平在全天持续分层**；各组的累计成交额时间分布则相当接近。大量净申购组并不普遍表现为“一次冲高后大幅回落”。

下表是发现期各组的基金日中位数：

| 因子 | 大量净申购 | 小额净申购 | 份额不变 | 小额净赎回 | 大量净赎回 |
|---|---:|---:|---:|---:|---:|
'''
show=[('settlement_mean_bp','平均结算溢价 / bp',1),('settlement_p10_bp','结算溢价10%分位数 / bp',1),('positive_amount_share','正溢价成交额占比 / %',100),('lag_flow_pct','昨日净份额变化 / %',1),('turnover_pct','成交额 / 昨日资产 / %',1),('premium_vs_20d_bp','较自身此前20日溢价变化 / bp',1)]
for f,label,scale in show:
 vals=[]
 for g in ['large_create','small_create','flat','small_redeem','large_redeem']:
  v=next(x for x in cohorts if x['feature']==f and x['cohort']==g);vals.append(f"{float(v['median'])*scale:.2f}")
 s+='| '+label+' | '+' | '.join(vals)+' |\n'
s+='''
这里的“正溢价成交额”采用结算溢价>10基点的分钟成交额。10基点是噪声缓冲门槛，不代表实际交易成本。

由此可归纳出四点：

**第一，溢价的低位比一次最高点更有解释力。** 大量净申购组的10%分位数仍为+10.62基点，意味着该组典型基金日绝大部分有效分钟保持正溢价。份额不变和净赎回组的低位明显在零以下。

**第二，应看成交发生在哪种溢价状态。** 大量净申购组正溢价成交额占比中位数88.84%，份额不变组只有1.99%。单纯的成交额/昨日资产中位数却相近，放量本身不能说明净申购。

**第三，净流量存在延续性。** 大量净申购组前一日净份额变化中位数+0.79%，大量净赎回组为-0.31%。这也解释了此前“均值溢价+前日净申购”规则为什么有效。

**第四，和基金自己的历史相比也有信息。** 大量净申购组当天平均溢价比自身此前20日中位数高10.65基点；大量净赎回组低13.92基点。因此仅采用全市场统一绝对门槛会遗漏部分基金的变化。

![因子共性对照](results/reverse_factor_heatmap.png)

热图数字为“组中位数与全体中位数之差 / 全体四分位距”，用于统一不同量纲，不能解释成概率或因子系数。颜色截断在±2，格内数字保留原值。

## 3. 可以落地计算的因子

设分钟结算溢价 `p_t=(ETF_t/结算IOPV_t−1)×10000`（bp），分钟成交额为`a_t`，昨日基金资产为`A`。仅计算ETF和港股共同有效交易分钟；午休、15:00之后排除。

| 因子 | 计算方式 | 用途与判断 |
|---|---|---|
| 全天结算溢价水平 | `mean(p_t)`、`quantile(p_t,0.1)` | 核心：水平与持续低位 |
| 成交额加权溢价 | `Σ(a_t p_t)/Σa_t` | 核心：过滤少成交的高溢价分钟 |
| 正溢价成交额占比 | `Σa_t·1[p_t>10]/Σa_t` | 保留：溢价是否伴随真实成交 |
| 申购方向压力 | `100Σa_t·max(p_t−10,0)/A` | 保留作候选，单位为bp×成交强度百分比；不是申购数量 |
| 赎回方向压力 | `100Σa_t·max(−p_t−10,0)/A` | 保留作相反方向的候选 |
| 溢价回落成交强度 | `100Σa_t/A`，只计溢价下降>10bp、前分钟溢价>30bp、ETF自身价格下跌的连续分钟 | 诊断卖压形态；仍不能确认是申购卖盘 |
| 相对自身历史溢价 | 当日均值减去此前20个有效基金日均值的中位数 | 保留：去掉一部分长期固定溢价差异，至少5个历史观测 |
| 当日横向溢价排名 | 当天可估值基金中平均溢价百分位 | 保留：相同市场背景下的相对位置 |
| 历史净流量 | 前一日、此前5个记录日净份额变化 | 核心：状态延续性 |
| 单纯成交强度、篮子涨幅 | 全天成交额/昨日资产、篮子涨跌 | 本轮没有成为稳定核心因子 |

压力因子和已有溢价因子高度相关，不能把多个相关指标相加就理解成多份独立证据。现有筛选模型保留了49列，包含相近的分位数、时长和中间价版本；后续产品化宜围绕上述少数核心族压缩，不能把49列误读成49个独立有效因子。

### 相关性跨期是否保持

下表均与当日净份额变化百分比计算Spearman相关。发现期“同基金内”先减去每只基金自身中位数，再计算相关；它是稳健性检查，不是严格的双向固定效应回归。

| 因子 | 发现期 | 同基金内 | 1—3月 | 新验证段 |
|---|---:|---:|---:|---:|
'''
fnames={'settlement_mean_bp':'平均结算溢价','settlement_p10_bp':'溢价10%分位数','settlement_amount_weighted_bp':'成交额加权溢价','positive_amount_share':'正溢价成交额占比','creation_pressure':'申购方向压力','compression_turnover_pct':'回落成交强度','premium_vs_20d_bp':'较自身20日溢价变化','lag_flow_pct':'昨日净流量','turnover_pct':'单纯成交强度','relative_turnover_20d':'相对20日成交强度','basket_return_bp':'篮子日内涨跌'}
for f,label in fnames.items():
 r=factors[f];s+=f"| {label} | {float(r['train_rho']):.3f} | {float(r['within_fund_rho']):.3f} | {float(r['validation_rho']):.3f} | {float(fresh[f]['fresh_rho']):.3f} |\n"
s+='''
新验证段方向是验证后展示的诊断，不再反向改筛选。没有将相关系数当成交易收益或因果效应。

## 4. 正向推算：合理因子能否识别结果

### 4.1 少量高命中率条件

发现期提出固定规则网格，在1—3月要求至少100个基金日、10只基金、20个日期，按命中率选择。净申购与大量净申购两个目标都选中了：

**平均结算溢价>30bp，正溢价成交额占比≥80%，且前一日净申购。**

新验证结果：

| 条件 | 入选基金日 | 净申购 | 大量净申购 | 净赎回 |
|---|---:|---:|---:|---:|
| 全部新验证样本 | 3,830 | 16.1% | 6.1% | 29.6% |
| 原有规则：平均溢价>30bp且前日净申购 | 78 | 84.6% | 52.6% | 2.6% |
| 新规则：再要求正溢价成交额占比≥80% | 75 | 84.0% | 52.0% | 2.7% |

**新规则没有优于原有规则。** 原有规则仍值得保留；不能因为增加了一个有相关性的因子，就宣称策略改善。新规则39个大量净申购样本只覆盖新验证段235个此类事件的16.6%，所以高命中率来自较窄的筛选范围，不能和宽覆盖模型直接比较高低。

### 4.2 宽覆盖的评分模型

为避免只比较某个阈值，另外报告AP：它衡量从高分到低分的整体筛选质量，越高越好；本报告实际计算的是average precision，不是梯形积分版PR-AUC。

三个目标独立建模：任何净申购、大量净申购、大量净赎回。每个目标统一比较历史变量、基础因子、此前全部因子、新筛选因子；模型复杂度相同。

| 目标 | 模型 | 入选数 | 命中率 | 覆盖该类事件 | AP |
|---|---|---:|---:|---:|---:|
'''
mn={'history_only':'仅历史净流量','simple_levels':'均值等基础因子','prior_full':'此前全部因子','enriched':'反向筛选后因子'}
tn={'flow_positive':'净申购','large_positive':'大量净申购','large_negative':'大量净赎回'}
for target in tn:
 for name in mn:
  r=next(x for x in models if x['period']=='fresh' and x['target']==target and x['model']==name)
  s+=f"| {tn[target]} | {mn[name]} | {r['n']} | {pct(r['precision'])} | {pct(r['recall'])} | {float(r['ap']):.3f} |\n"
s+='''
![新增日期正向检验](results/fresh_validation.png)

新模型的净申购筛选覆盖114只基金、22个日期，777个入选样本中451个净申购；命中率58.0%，按交易日聚类的95%区间53.5%—62.4%。大量净申购模型选出852个基金日，其中219个大量净申购，覆盖235个此类事件的93.2%，但命中率只有25.7%。这两个目标必须分开解读。

冻结的是6月评分阈值，所以新验证段入选比例约20%—22%，不是固定10%。6月到新验证段基准概率变化，不能把阈值直接解读成“90%置信度”。模型评分均值与实际发生率、评分分桶校准表一并保存；未把原始分数宣称为已校准的执行概率。

### 4.3 新因子究竟有没有增量

相对此前全部因子，新模型AP的变化为：

- 净申购：+0.0098，按日期成对重采样95%区间约[-0.0001, +0.0213]。
- 大量净申购：+0.0025，区间[-0.0194, +0.0225]。
- 大量净赎回：+0.0246，区间[+0.0050, +0.0458]。

因此，**当前证据不足以断言新增因子显著改善了净申购识别**；大量净赎回方向的增量更明确，但仍只经过22个新交易日，且没有做多重比较校正。

消融检验也支持控制复杂度。大量净申购模型AP从0.524降为：去掉历史变量0.461、去掉压力类变量0.522、去掉相对历史/横向位置变量0.521。历史净流量的增量远大于新增的压力类变量。它们不是完全独立贡献，去掉一个高度相关因子组后，其余因子仍可能替代其信息。

![历史逐月前推](results/rolling_validation.png)

每月前推都优于只看总体发生率，但随着市场状态变化，绝对命中率明显下降。反向筛选模型并非每个月都优于基础模型，不应只引用表现最好的月份。

## 5. 能不能直接预测净增多少篮子

另外用相同价格因子回归当日净份额变化百分比，再用昨日份额/申赎单位换算净篮子。预测过程不使用当日净量；当日标签只用于检验误差。

| 方案 | 净份额变化MAE / 百分点 | 净篮子MAE | 与实际净变化的Spearman |
|---|---:|---:|---:|
| 全部预测为0 | 0.806 | 23.93 | 不适用 |
| 直接沿用昨日变化比例 | 1.124 | 23.52 | 0.389 |
| 基础因子模型 | 0.783 | 19.95 | 0.526 |
| 反向筛选后模型 | 0.783 | 20.21 | 0.530 |

MAE是所有新验证基金日的平均绝对误差。新模型方向排序有信息，但相对于全预测0，净份额比例误差仅小幅降低；净篮子平均误差仍约20篮子。**目前适合做方向/机会排序，不适合据此精确估计轧差比例或结算收益。** 大基金和小基金篮子价值不同，篮子误差不能直接换算成统一人民币风险。

## 6. 数据质量、覆盖与仍未解决的问题

本轮共同使用36,277个基金日。横向排名以本研究质量筛选后的可估值池计算；未来复用时需固定当日可计算的标的池，不能按事后份额标签是否齐全改变池子。原区间32,447个，新增3,830个。当前标的名单来自[1navs板块](https://1navs.com/funds/yesterday-redemption-board)，实际样本还要通过PCF、港股成分、ETF行情、份额标签对齐等检查，存在存续标的和缺失样本选择偏差。

新验证段做了4个日期的独立PCF上一日净值核对：561个对象，529个可计算，绝对差异中位数0.284bp，95%分位数6.55bp，99%分位数26.99bp。520660在2026-07-15偏差-104.06bp，超过100bp，尚未定位为公司行动还是数据口径异常。没有隐瞒这项问题：不改模型，单独剔除该基金日或整只基金做敏感性。大量净申购模型AP由0.5236分别变为0.5247、0.5259，主要结论未依赖这个异常样本；该基金仍应在实际估值使用前核对。

净值核对是抽样，不意味着所有成分、所有分钟都已验证。此前发现的必须现金替代缺字段、缺成分行情等问题继续按原规则排除。新增ETF分钟档案仅支持至8月3日；PCF有部分更晚日期，并不足以单独延长完整回测。

日内汇率固定为当日最终结算率与中间价，全天结束后完整路径已知。因此这次“正向”指时间上用过去训练、对后面的交易日检验，不代表当天早盘已经能算出全天因子。用户本轮不要求盘中汇率预测，故没有改动该口径。15:00之后和午休始终不进入ETF溢价因子。

没有逐笔委托、申购成交时间或双边总量，不能从这些因子确认某次价格回落就是申购卖盘。市场行为特征是价格和成交额代理变量，不是参与者行为的直接观测。

## 7. 后续可直接复用的内容

建议保留两种输出：

- 少量高命中率观察名单：继续保留原有“全天结算溢价>30bp且前日净申购”规则。本次复杂化没有改善它。
- 较广覆盖的排序名单：固定模型分别给出净申购、大量净申购、大量净赎回评分。核心维护溢价水平/低位、成交额加权溢价、历史净流量，再保留相对自身溢价变化；压力类和回落特征继续作为待积累样本的候选。

不因本次22天结果反复调整阈值。下一步最有价值的是补齐8月ETF分钟行情及缺失PCF，继续用本次已保存模型检验新日期；而非在已经看过的月份继续寻优。

### 文件与复算

- [反向因子证据](results/reverse_factor_evidence.csv)、[分组原始统计](results/reverse_cohorts.csv)：每个候选因子的方向、稳定性、组内分布。
- [固定模型定义](results/frozen_spec.json)、[规则选择](results/frozen_rule_selection.json)：在新验证结果前固定的特征与参数。
- [正向模型比较](results/forward_models.csv)、[历史逐月结果](results/rolling_months.csv)、[新样本评分](results/fresh_predictions.parquet)。
- [增量区间](results/incremental_ap_bootstrap.json)、[质量敏感性](results/quality_sensitivity.csv)、[数量预测误差](results/quantity_regression.csv)。
- [完整特征表](results/enriched_panel.parquet)：包含所有有效基金日；当日标签与模型特征列分开，模型按白名单取列。

代码位于父目录 `factor_research`：`extend_reverse.py`、`reverse_features.py`、`reverse_analysis.py`、`reverse_checks.py`、`reverse_robustness.py`、`reverse_charts.py`、`score_reverse.py`。运行环境版本保存为 `requirements_snapshot.txt`。

在machome研究根目录依次运行：

```sh
.venv/bin/python factor_research/extend_reverse.py
.venv/bin/python factor_research/reverse_features.py
.venv/bin/python factor_research/reverse_features.py --extension
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=2 .venv/bin/python factor_research/reverse_analysis.py
.venv/bin/python factor_research/reverse_checks.py
.venv/bin/python factor_research/reverse_robustness.py
.venv/bin/python factor_research/reverse_charts.py
.venv/bin/python factor_research/write_reverse_report.py
```

`score_reverse.py 输入特征表.parquet 输出评分.csv` 可调用本次固定模型。输入必须先按同口径计算全天特征、历史滚动值和当日横向排名，不能只传ETF代码和日期。它不需要输入当日净申赎标签。已用删除当日标签的20行输入样例成功评分，并核对：追加未来日期不会改变此前滚动特征，改写当日结果标签不会改变任何模型输入。模型序列化文件在本目录，仅加载自己保存的可信模型文件。
'''
(D/'反向因子分析与正向验证.md').write_text(s)
print('Report written')
